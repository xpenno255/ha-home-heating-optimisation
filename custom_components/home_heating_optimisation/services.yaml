record_adjustment:
  fields:
    note:
      required: true
      selector:
        text:
          multiline: true
    kind:
      default: other
      selector:
        select:
          options:
            - lockshield
            - boiler_setting
            - sensor_change
            - other
    room_id:
      selector:
        text:
get_report:
get_house_model:
reload_house_model:
run_review:
  fields:
    task:
      required: true
      selector:
        select:
          options:
            - daily_summary
            - weekly_review
            - investigation
    question:
      selector:
        text:
          multiline: true
get_advisor_reports:
  fields:
    report_id:
      selector:
        text:

preview_control_import:
import_controls:
get_control_report:
handover_controls:
rollback_controls:
preview_identity_migration:
migrate_identities:
  fields:
    confirm:
      required: true
      selector:
        boolean:
rollback_identity_migration:
