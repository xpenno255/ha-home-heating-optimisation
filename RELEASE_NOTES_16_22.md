- Investigated same-temperature radiator renewals (#16): confirmed against ramses_rf 0.60.5 and ramses_cc 0.60.5/master that Home Assistant receives no inbound packet provenance for zone mode state, so such renewals stayed explicitly `matching_readback_unverified`; documented the findings, a narrowly scoped upstream capability request and HHO's feature-detect compatibility path in `docs/renewal-acknowledgement-2026-09-20.md`.
- Added a `readback_hint` attribute to the room decision sensor and control report for `matching_readback_unverified` and `readback_timed_out` outcomes, explaining what the status means and what to check without claiming failed delivery or valve movement; confirmation rules, manual/off handling and the bounded reversion retry were unchanged.
- Added a "Same-temperature renewals" section to `docs/command-confirmation.md`.
- Published the design (not implementation) for optional bounded coordination and learning (#22) in `docs/coordination-and-learning-design.md`: learnable parameter list with per-parameter flags, hard bounds and provenance; layer separation; eligibility and abstention rules; replay/shadow validation before any #21 trial; rollback; learning-version recording; metered-only energy claims; phased plan with tests.
